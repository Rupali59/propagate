# Systems Ledger

> Entry point: [`../SKILL.md`](../SKILL.md) · Index: [`README.md`](./README.md)

Append-only record of every automated/background component in the ecosystem.
This is the adoption gate: a component is not "done" when it works, it is
done when it has a record here with a real liveness probe and, eventually,
an adoption date. See `docs/DECISIONS.md` 2026-08-10 "the local web UI is cut" —
this file exists because that decision's whole argument was "we shipped
things and never checked whether anyone used them."

Columns:
- **id** — stable slug, referenced elsewhere.
- **kind** — launchd agent / CLI / MCP-adjacent tool / library.
- **status** — `active` | `active-unadopted` | `proposed` | `dormant` | `retired` |
  `installed-never-invoked`.
- **supersedes / superseded_by** — lineage, blank if none.
- **artifacts** — paths, launchd labels, ports. This is where to look before
  believing a status claim.
- **liveness_probe** — the actual command to run to check current state
  yourself. Not prose — a command.
- **last_verified** — date the probe was last actually run (not the date the
  component was built).
- **adoption_date** — date a human confirmed real, repeated use. Blank means
  not yet earned, regardless of how long the component has existed.
- **retirement_checklist_done** — per `.templates/NEW-CLIENT-CHECKLIST.md`'s
  retirement counterpart (hub `DECISIONS.md` 2026-08-10 "retirement gets a
  checklist"): unload+disable LaunchAgent, remove code-review-graph registry
  entry, remove `.propagates.yml`, update hub repo map, record here.

---

| id | kind | status | supersedes / superseded_by | artifacts | liveness_probe | last_verified | adoption_date | retirement_checklist_done |
|---|---|---|---|---|---|---|---|---|
| `propagate` | launchd agent (Node, StartInterval 60) | **retired 2026-08-14** | superseded_by: `reconcile` (on demand) + `check` (pre-push) + the daily digest's DRIFT/INBOUND sections — see `docs/DECISIONS.md` 2026-08-14 for the evidence (4,420 runs / 4,384 no-ops, 99.2%) and the full tradeoff (sub-daily proactive notification is genuinely lost). None of these three replacements are themselves launchd agents — they're on-demand/CLI, so this row has no direct successor row in this table; that absence is the point, not an omission. | `docs/archive/com.tathya.propagate.watcher.plist.retired-2026-08-14` (removed from LaunchAgents 2026-08-14, archived so the retirement is reversible); `~/.claude/skills/propagate/heartbeat` (now stale by design); `watcher.mjs` (kept on disk, header records the retirement, refuses to run directly without `PROPAGATE_ALLOW_RETIRED_WATCHER=1`) | Retired: heartbeat age is no longer a health signal (`cli.mjs doctor` and `digest.mjs` both stopped treating it as one 2026-08-14 — see the DECISIONS.md entry). The replacement's liveness probe is now: `node cli.mjs doctor` → "v2 replacement" section → "event store readable" + "reconcile completes" both green. | 2026-08-14 (code+docs side of retirement verified: `cli.mjs doctor` no longer reports watcher checks as failures, reports replacement health instead; 470/470 tests pass) | 2026-08-10 (watcher itself, historical — retired 2026-08-14, see status column) | **done** — code path retired, `doctor`/`digest.mjs` updated, `SKILL.md`/`docs/REFERENCE.md`/`docs/ISSUES.md` updated, this row updated, all in this change. The launchd unload was completed 2026-08-14 in the same session: job booted out, plist removed and archived, verified that only the digest remains loaded and no watcher process runs. `.propagates.yml` removal / hub repo map update are not applicable (the watcher was never itself a client workspace). |
| `propagate-digest` | launchd agent (Node, StartCalendarInterval 09:00) | active | — | `~/Documents/GitHub/... /propagate/digest.mjs`; installed as `~/Library/LaunchAgents/com.tathya.propagate.digest.plist`; state at `~/.claude/propagate-digest-state.json`; delivery to `~/.claude/DAILY.md` + osascript notification. **Naming discrepancy resolved 2026-08-20:** the committed root-level plist under the old `com.rupali.*.propagate-digest.plist` naming this row used to flag was already removed from the repo in `815dd73` ("drop the stale plist") — checked directly against `git log --diff-filter=D`, the file predated this row's own "flagged for reconciliation" note. **Generator added 2026-08-20 (P3):** until now this was the one plist `lib/core/plist.mjs` never wrote — hand-maintained, hardcoding `/opt/homebrew/bin/node` and the skill's absolute path by hand. `writeDigestPlist()` (`lib/core/plist.mjs`) generates it now, wired via `node digest.mjs --install` (writes the file only, mirrors `monitor --install`'s contract — never touches launchd). Regenerated on this machine 2026-08-20; log paths moved from `/tmp/propagate-digest.log` to `~/.propagate/digest.{stdout,stderr}.log` for consistency with the monitor. | `launchctl list \| grep com.tathya.propagate.digest` (expect a loaded entry) AND `tail -1 ~/.claude/DAILY.md` shows a run newer than 25h ago AND `~/.propagate/digest.stderr.log` has no fatal lines since last install | 2026-08-13 (verified directly: plist loaded via `launchctl list`, `~/.claude/DAILY.md` has an entry timestamped `2026-08-13T03:57:19Z`, `~/.claude/propagate-digest-state.json`'s `lastRunAt` matches) | **BLANK — do not fill until 2 weeks of demonstrated engagement with the digest are observed. Installed-and-running (confirmed 2026-08-13) is not the same claim as adopted.** | n/a |
| `queue-ui` | Python HTTP server (LaunchAgent) | **active-unadopted** | — | `~/Library/LaunchAgents/com.tathya.claude-queue-ui.plist`; `~/.claude/pending-queue.json`; `~/.claude/queue-ui.token`; `claude-queue-ui.py` | `curl -s -o /dev/null -w '%{http_code}' http://localhost:8790/` (process reachable) — reachability is NOT the same as use; `claude-queue-ui.py:255` (`def log_message(self,*a): pass`) makes it structurally unable to log a hit, so this probe can only prove the process is up, never that anyone used it | 2026-08-10 | **BLANK** | not started |
| `queue-runner` | Node CLI + LaunchAgent (`claude-next.sh`) | dormant | — | `scripts/claude-next.sh`; `scripts/claude-queue-onboard.sh`; `~/.claude/pending-queue.json` (37 items) | `python3 -c "import json;print(json.load(open('/Users/rupali.b/.claude/pending-queue.json')).get('armed'))"` — expect `false` | 2026-08-10 | n/a (never adopted) | not started |
| `cross-repo-relay` | Node module, fires from `propagate` watcher | dormant | — | `PROPAGATION_CROSS_LEDGER.jsonl` (5 rows), `PROPAGATION_CROSS_LEDGER.md`, `lib/edges/cross-repo.mjs` | `node cli.mjs status --cross` — expect the single 2026-07-13 burst and silence since | 2026-08-10 | n/a (never adopted) | not started |
| `md-watcher` | Python script + LaunchAgent | retired | → nothing (dead end, not replaced) | `~/Library/LaunchAgents/com.markdown.watcher.plist` (still present, mtime 25 Mar); target `~/.local/bin/markdown_watcher/markdown_watcher.py` (still present, executable, mtime 25 Mar — **the plist does NOT point at a missing file**, this is subtler than "dead"); `~/Library/Logs/markdown_watcher.stdout.log` **24,164,062 bytes**, `markdown_watcher.log` 24.2MB, both last written 25 Mar; NOT currently loaded (`launchctl list \| grep markdown` returns nothing) | `launchctl list \| grep com.markdown.watcher` (expect empty = not loaded) AND `stat -f %Sm ~/Library/Logs/markdown_watcher.stdout.log` (expect a March date, not recent) | 2026-08-10 (verified directly: plist exists, script exists, not loaded, logs are the 22.7MB stale artifact, unchanged since March) | n/a | **not done** — LaunchAgent plist itself was never removed, only unloaded; script and 22.7MB logs still on disk |
| `chaukidar-utility` | Node/shell utility (unrelated to Motherboard's `chaukidar` service) | retired 2026-08-10 | — | source lost (could not be relocated at retirement time); wrote 37MB of stderr over 6 weeks before being stopped | n/a — source gone, nothing to invoke | 2026-08-10 | n/a | done (crash loop stopped; see task #1 in session history) |
| `chaukidar-motherboard` | Go service, `motherboard-coordination/services/chaukidar` | active | unrelated domain to `chaukidar-utility` above — name collision only | `Motherboard/motherboard-coordination/services/chaukidar` | `docker compose ps chaukidar` inside `Motherboard/` (or the service's own health endpoint per Motherboard's own conventions) | 2026-08-10 (path confirmed to exist in code-review-graph's registered repo list) | pre-existing, in production use | n/a |
| `harry-potter-theme` | LaunchAgent, unknown original purpose | retired 2026-08-10 | — | target was under `Utility/` which is itself archived (`_archive/2026-06-28/`); LaunchAgent exited 127 (command not found) | n/a — target archived, agent removed | 2026-08-10 | n/a | done (LaunchAgent stopped; see task #10) |
| `code-review-graph` | CLI (`~/.local/bin/code-review-graph`) + per-repo `.code-review-graph/graph.db` + MCP server | active | — | `~/.code-review-graph/registry.json`; per-repo `.code-review-graph/graph.db` (6 built, confirmed via `repos` subcommand listing Motherboard services + Vipin Kaushik projects); **hub graph at `~/Documents/GitHub/.code-review-graph/graph.db` exists on disk (151KB) but `status` reports it near-empty** — 18 nodes / 308 edges / 3 files as of 2026-07-16, JS/TS repos registered but not rebuilt since; registry has entries pointing at repos not all still present | `~/.local/bin/code-review-graph status` (per-repo, run from inside each registered repo) | 2026-08-10 (ran `status` and `repos` directly) | in active use for Python repos | n/a |
| `ollama` | local model runtime | installed-never-invoked | — | `gemma4:latest` (9.6GB), `nomic-embed-text:latest` (274MB), both pulled ~3 months ago per `ollama list` | `ollama ps` (expect empty — nothing currently loaded) and `ollama list` (models present but `MODIFIED` timestamp is the pull date since these are static, not evidence of use — check actual invocation via shell history/logs if a real usage question comes up) | 2026-08-10 | never | n/a |
| `skill-registry` | Node lib + CLI modes in `propagate`, reaper rides the existing digest | active | — | `lib/skills/skills-scan.mjs`, `lib/skills/skills-lifecycle.mjs`, `lib/skills/skills-create.mjs`; `~/Documents/GitHub/skills-marketplace/`; `SKILLS_LIFECYCLE.jsonl`; `skill` table in `index.db`; kill switch `~/.claude/skills-registry.off` | `node cli.mjs skills` runs and classifies every skill by provenance (gstack-relink / handmade / npx-skills) and reports both lifecycle tiers; `node cli.mjs skills-reap` names only quarantined skills unused for >14d. **The probe is that it classifies, not a count** — the total was 169 when this was planned, 92 when the row was written and 61 on 2026-08-17, because skills are consolidated continuously. Naming the number here is how a state file rots (rule:state-and-decisions). | 2026-08-11 | 2026-08-16 — `public-site-audit` completed the full quarantine -> promoted path without hand-holding (`SKILLS_LIFECYCLE.jsonl`); it now lives at `skills-marketplace/tathya/skills/public-site-audit`. Two skills remain in quarantine and both register live as `quarantine:changelog-entry` and `quarantine:env-pull-check`. Nothing has been reaped yet, which is expected — neither quarantined skill is both unused and older than 14d. | n/a |
 | `gotcha-guard` | PreToolUse hook (Node), matchers `Bash` and `Edit\|Write`. No daemon, no schedule — it runs only when a tool call is made | active | — | `~/.claude/hooks/gotcha-guard.mjs`; index `~/.claude/gotchas-global.md` plus every `docs/GOTCHAS.md` from cwd upward; log `~/.claude/gotcha-guard.log`; registered in `~/.claude/settings.json` | `node --test ~/.claude/hooks/` (9 behaviour tests over the real stdin/stdout interface) AND `node ~/.claude/hooks/gotcha-guard.mjs --selftest` (index integrity) AND `tail ~/.claude/gotcha-guard.log` (one line per invocation with `hits=N`, so "never fired" and "never ran" differ). **All three are needed.** The selftest alone shipped a `.slice(0, 6)` that dropped every entry's `**Instead:**` remedy — it validated the index and never looked at what the operator receives. | 2026-08-17 | **BLANK — fill only when a logged `hits=1` corresponds to a mistake somebody did not then make.** Firing is not the same as helping, and this is the component most likely to become decorative. | n/a | 
 | `propagate-monitor` | launchd agent (Node). `WatchPaths` on discovered workspace roots, `ThrottleInterval` 300, `StartInterval` 1800 (48 runs/day, against the retired watcher's 1,440) | **ARMED 2026-08-17** — loaded via `launchctl bootstrap`; first production run 5088ms over 724 edges, 8 notified, exit 0 | — | `lib/report/monitor.mjs`, `cli.mjs` `monitor` mode, `lib/core/plist.mjs` `writeMonitorPlist`; state `~/.propagate/notified.jsonl` + `~/.propagate/monitor.log`; label `com.tathya.propagate.monitor`, deliberately NOT the retired watcher's. **Node path fixed 2026-08-20 (P3):** the installed plist pinned `ProgramArguments` to `/opt/homebrew/Cellar/node/25.9.0_1/bin/node` — `process.execPath` resolving symlinks, not the plist being hand-edited — so it was one `brew upgrade node` away from launchd failing to exec with no printed reason. `writeMonitorPlist` now goes through `resolveStableNodeBin()` (`lib/core/plist.mjs`), which prefers `command -v node`'s unversioned symlink and falls back to `process.execPath` only if that lookup fails; regenerated on this machine, now reads `/opt/homebrew/bin/node` | `tail ~/.propagate/monitor.log` distinguishes THREE states, which is what makes it a probe: **never ran** (no file), **ran and found nothing** (`notified=0`), **ran and told you** (`notified>0`). Whether it is armed is a separate question, and `monitor --install` prints the exact arm/disarm commands rather than this row carrying them — a row naming an uninstalled label beside a shell command is a line someone copies and is refused by tests/docs/skill-doc.test.mjs. A monitor that has silently died is indistinguishable from a quiet week without that log — the same absence that let a zombie LaunchAgent write 37 MB of stderr over six weeks. **CLOSED 2026-08-22, after this row's own prediction came true** (N43: the plugin cutover repointed the plist at a deleted path; the agent failed every 1800s for hours while `doctor` printed "all green", and it was found by chance running `ls`). `doctor` now carries `check("monitor is running on schedule")`: gated on the PLIST existing (armed, not merely generated — that distinction is what makes a check() safe here, since an unarmed monitor must never fail a fresh install), it fails when the last successful run is older than 3x the 1800s interval, and separately names a monitor that is **loaded and crashing** by comparing `monitor.stderr.log`'s mtime against `monitor.log`'s — `monitor.log` records only SUCCESSES, so that one comparison is the only on-disk tell. Five tests, three of them negative controls; mutating the guard turns exactly the two positive ones red. **This row is the reason the fix was framed correctly**: it described the failure accurately and detected none of it, which is `rule:enforcement-watches-itself`'s stated signal — you can describe the failure fluently in a comment, and the fluency is what makes it feel handled. | 2026-08-17 | **BLANK — fill only when a notification it sent led to an edge being resolved sooner than the daily digest would have.** Firing is not helping. This is the component most likely to become the thing everyone swipes away, and the v1 watcher earned its retirement on exactly that distinction (4,420 runs, 99.2% no-ops). | n/a | 
| `rules-loader` | SessionStart hook (Node) | **active-unadopted** | — | `~/.claude/hooks/load-rules.mjs`; registered in `settings.json` `hooks.SessionStart`; rules at `~/.claude/rules/*.md` | `node ~/.claude/hooks/load-rules.mjs \| grep -c additionalContext` (1 = emitting; the hook also shouts when it loads 0 rules) | 2026-08-14 | — *not earned* | n/a |
| `rules-duplication-check` | CLI (Node) | **active-unadopted** | — | `~/.claude/rules/_check.mjs` | `node ~/.claude/rules/_check.mjs; echo $?` — exit 1 means restatements exist. Also `--selftest` proves each fingerprint can fire | 2026-08-14 | — *not earned* | n/a |
| `inventory-probe` | CLI + digest section | **active-unadopted** | — | `lib/report/inventory.mjs`; `cli.mjs inventory`; INVENTORY block in `digest.mjs` | `node ~/.claude/skills/propagate/cli.mjs inventory \| head -3` | 2026-08-14 | — *not earned* | n/a |
| `skills-marketplace` | directory tree + lifecycle CLI | **installed-never-invoked** | — | `~/Documents/GitHub/skills-marketplace/{tathya,quarantine}/skills`; `lib/skills/skills-lifecycle.mjs`; `SKILLS_LIFECYCLE.jsonl` | `ls ~/Documents/GitHub/skills-marketplace/tathya/skills \| wc -l` → 0. **Manages a tree the 92 installed skills do not live in** — its 3 pre-2026-08-14 log events are two test skills | 2026-08-14 | — *not earned* | n/a |
| `skills-archive-2026-08-14` | archive (31 skill dirs) | retired | supersedes: 31 skills with `usageCount==0` | `~/.claude/skills-archive/2026-08-14-never-invoked/` + `RESTORE.md` | `ls -1d ~/.claude/skills-archive/2026-08-14-never-invoked/*/ \| wc -l` → 31 | 2026-08-14 | n/a | **done** — nothing deleted; restore is a `mv` |
| `gstack-checkpoint` | skill (dangling symlink) | retired | — | `~/.claude/skills/gstack-checkpoint/SKILL.md` → `gstack/checkpoint/SKILL.md` (**target missing**) | `[ -e ~/.claude/skills/gstack-checkpoint/SKILL.md ] && echo live \|\| echo dangling` | 2026-08-14 | n/a | **pending** — dir still present, symlink broken |
| `gemastrology-shopify` | git repo | active | — | `~/Documents/GitHub/PanditPawanKaushik/gemastrology-shopify` | `git -C … rev-list --all --count` → **7 commits**, `git remote -v` → `github.com/Rupali59/gemastrology-shopify.git`, 0 dirty. **Re-verified 2026-08-14: backed up by Rupali.** Was 0 commits / no remote / 29 loose files earlier the same day — the row is kept rather than deleted so the transition is legible | 2026-08-14 | n/a | n/a |
| `gemastrology-theme` | git repo | active | — | `…/PanditPawanKaushik/gemastrology-theme` | `git remote -v` → `github.com/Rupali59/gemastrology-theme.git`; 6 commits, 0 dirty. **Re-verified 2026-08-14** — had no remote earlier the same day | 2026-08-14 | n/a | n/a |
| `form-collector` | git repo | active | — | `…/PanditPawanKaushik/form-collector` | `git log --branches --not --remotes --oneline \| wc -l` → **21 unpushed**, no remote | 2026-08-14 | n/a | n/a |
| `rupali-varga-charts` | analysis workspace | active | — | `~/Documents/GitHub/rupali-varga-charts` | `[ -e …/.git ] \|\| echo 'NO GIT AT ALL'` → 30 files, never version-controlled | 2026-08-14 | n/a | n/a |
| `motion-graphics` | git repo (Remotion) | dormant | — | `~/Documents/GitHub/Motion-Graphics` | `git remote -v` → none; 2 commits, both unpushed | 2026-08-14 | n/a | n/a |
| `tushar-workspace` | git repo | active | — | `~/Documents/GitHub/Tushar` | 15 commits, **all 15 unpushed**, no remote | 2026-08-14 | n/a | n/a |
| `github-hub` | git repo (the tree root) | active | — | `~/Documents/GitHub` | 5 commits, **no remote**. The hub's own no-remote inventory does not list itself | 2026-08-14 | n/a | n/a |
| `khushboo-workspace` | doc-only husk | dormant | superseded_by: archived 2026-06-28 | `~/Documents/GitHub/Khushboo` (`CLAUDE.md`, `docs/`, `persona/`; no repos) | `persona/profile.yaml` still says `status: active` — profile and reality disagree | 2026-08-14 | n/a | **pending** |
| `rishabh-workspace` | doc-only husk | dormant | superseded_by: `Rishta-mb` archived 2026-06-28 | `~/Documents/GitHub/Rishabh` | same: profile says `active`, no code present | 2026-08-14 | n/a | **pending** |
| `daily-md` | generated artifact | **installed-never-invoked** | — | `~/.claude/DAILY.md` (written by `propagate-digest`) | `grep -rl DAILY.md ~/.claude/CLAUDE.md ~/Documents/GitHub --include=CLAUDE.md \| wc -l` → **0 readers** | 2026-08-14 | — *not earned* | n/a |
| `ports-yml` | canonical registry | **active-unadopted** | supersedes: `apply-next-dev-rule.mjs` `PORT_DEFAULTS` | `~/Documents/GitHub/ports.yml` | `grep -rl ports.yml --include=CLAUDE.md ~/Documents/GitHub \| wc -l` → **0 referrers**, though it declares itself canonical. Now cited by `rule:nextjs-dev-server-port` | 2026-08-14 | — *not earned* | n/a |
| `disabled-plugins` | plugin set | dormant | — | `settings.json` `enabledPlugins` | `python3 -c "import json,os;d=json.load(open(os.path.expanduser('~/.claude/settings.json')))['enabledPlugins'];print(sum(1 for v in d.values() if not v),'of',len(d))"` → **28 of 48 disabled but retained** | 2026-08-14 | n/a | n/a |
| `obsidian-collect` | launchd agent (bash → Python, 22:00 daily) | active | superseded_by: `obsidian-journal` (proposed) | `~/Library/LaunchAgents/com.tathya.obsidian.collect.plist` (**loaded**); `Rupali/Obsidian/Scripts/bash/daily_auto_collect.sh`; in-repo template `Scripts/config/com.obsidian.dailycollect.plist` (**not** the loaded label) | `python3 -c "import json;rows=[json.loads(l) for l in open('…/Scripts/logs/collection_history.jsonl')];print(sum(1 for r in rows if (r['collectors'].get('github') or {}).get('commits',0)>0),'of',len(rows),'runs saw a commit')"` → **0 of 50**. Evidence preserved at `docs/archive/obsidian-collection_history.2026-05-23_2026-08-13.jsonl` — the source file is untracked and gitignored in its own repo (`.gitignore:23`, 0 commits), so retiring the collector would otherwise destroy the only copy and make this row's own claim unverifiable. Not broken: it queries `/search/commits`, which indexes default branches only, while the work is on feature branches (`collectors/github.py:86,91`) | 2026-08-14 | n/a | **handed over** — retirement specified in `Rupali/Obsidian/HANDOVER-2026-08-14.md`, executed by that repo. Hazard until then: `setup_automation.sh:8` hardcodes a different label, so `install` would add a SECOND agent on the same schedule |
| `worktracker-diary` | launchd agent (bash → bun, **23:00 daily**) | active | supersedes: `obsidian-collect` *in practice* — see note | `~/Library/LaunchAgents/com.tathya.diary.collect.plist` (**loaded**); `Tathya/WorkTracker/scripts/diary/daily.sh`; vault target `Tathya/WorkTracker/scripts/sync-manifest.json` → `../../Rupali/Obsidian` | `tail -3 "$(ls -1t ~/.work-diary/logs/daily-*.log \| head -1)"` → ends `===== daily diary done =====`. Writes `Rupali/Obsidian/Calendar/<Y>/<Month>/<DD-MM-YYYY>.md`, commits `diary: auto-update`, pushes, triggers CI → Quartz. Has locking, pre-flight abort on a dirty vault, gc and vault backups | 2026-08-15 | — *not earned* | n/a |
| `doc-structure` | CLI surface + PreToolUse hook + 2 calibrated expectations | active | supersedes: nothing — this is new | `lib/report/docs.mjs` (authority index, coverage) · `lib/report/doc-kind.mjs` (kind, supersession, broken-path citations) · `~/.claude/hooks/doc-authority.mjs` (registered in `settings.json`, `Edit\|Write`) · `cli.mjs docs [--all\|--kinds\|--structure\|--superseded]` | `node cli.mjs docs --structure` → per-project broken-citation counts; `docs --kinds` → the census; `doctor` asserts `docs.supersession_prose_only <= 107` (a ratchet) and `docs.supersedes_unresolvable == 0`. Hook proof: an Edit on a `counsel`-governed file exits 2 — verified on `VipinKaushik/app/(site)/privacy/page.tsx`, which exited 0 before the edge was declared | 2026-08-15 | — *not earned* | n/a |
| `obsidian-quartz-sync` | GitHub Actions workflow | active | — | `Rupali/Obsidian/.github/workflows/trigger-quartz-sync.yml` | `gh run list --workflow trigger-quartz-sync.yml -L 1`; fires on push to `Calendar/**`, `Notes/**`, `Vault.md` | 2026-08-14 | — *not earned* | n/a |
| `obsidian-worktracker-notify` | GitHub Actions workflow | active | — | `Rupali/Obsidian/.github/workflows/notify-worktracker.yml` | `gh run list --workflow notify-worktracker.yml -L 1`; fires on `Calendar/**`, `Notes/**` — **note: does NOT watch `Vault.md`, unlike its quartz sibling.** That divergence is the declared edge in `Rupali/Obsidian/.propagates.yml` | 2026-08-14 | — *not earned* | n/a |
| `obsidian-journal` | CLI + digest section (**proposed, not built**) | proposed | supersedes: `obsidian-collect` | planned `propagate/lib/edges/journal.mjs` and a `journal` subcommand (**not yet registered** — deliberately not written as a runnable invocation, so the docs never name a command a reader cannot run); sink `Rupali/Obsidian/Calendar/<YYYY>/<Month>/<DD-MM-YYYY>.md` | **No probe — the command does not exist yet.** Acceptance test when built: a `journal` subcommand must report **118** local commits across **24** repos for 2026-08-14, counted with `--since=2026-08-14T00:00:00+05:30 --until=2026-08-15T00:00:00+05:30`. **The window form is part of the test.** This figure was 95/21, then 109/22, then 63/10 before settling: 95 missed `propagate-skill` entirely (a symlink; `find` and `readdirSync().isDirectory()` both skip symlinked dirs), and the rest moved because a BARE `--until=2026-08-15` goes through git's approxidate parser and admitted commits stamped 2026-08-15. Verified by two independent implementations agreeing at 118/24 — the day `obsidian-collect` reported as 0 and `worktracker-diary` reported as 35 across 3 (figure corrected 2026-08-15; the earlier **40** was a partial count taken before every repo was enumerated). This row is `proposed`; it is a commitment, not a component. (Written first as a runnable invocation, which `tests/docs/skill-doc.test.mjs` correctly rejected — docs must not name commands that are not registered.) | 2026-08-14 | n/a | n/a |
| `stitch-vault` | git repo (JS project) | active | — | `Rupali/Artwork/crochet/stitch-vault`; 81 source files; **351 MB of `node_modules`** dominates the parent `Artwork/` 378 MB | `git -C … remote -v` → **none**; `rev-list --all --count` → **11**; `gh repo list Rupali59` (88 repos) → **absent**. Last commit **2026-08-10** — active work, not an archive. **Unbacked anywhere: not on GitHub, not in `_archive/`** | 2026-08-14 | n/a | n/a |
| `himk-thesis` | LaTeX thesis + assets | dormant | — | `Rupali/HIMK`; 125 files, 14 MB (`.tex`, 34 × `.png`, `Thesis14CSE2013.pptx`); carries a `.gitignore` and `.dockerignore` but **no `.git`** | `[ -e Rupali/HIMK/.git ] \|\| echo 'NEVER VERSION CONTROLLED'`; absent from GitHub and from `_archive/`. Set up for version control that never happened. Referenced in calendar entries from Feb 2026, so it is real work | 2026-08-14 | n/a | n/a |
| `rupali-archived-projects` | 6 former subdirectories | retired | superseded_by: GitHub remotes + `_archive/2026-06-28/` | `Cybernetics` (private, pushed 2026-02-23), `MIS` · `PaperBanana` · `Rupali59` (public, 2026-06-28) on GitHub; `rupali-ANN_Assignment.tar.gz` + `rupali-Courses.tar.gz` in `_archive/2026-06-28/` | `gh repo list Rupali59 --limit 200 --json name` and `ls _archive/2026-06-28/`. **All 6 accounted for — verified 2026-08-14, not assumed.** `Rupali/CLAUDE.md` still lists all 6 as present, and omits `InterviewReady/` which does exist | 2026-08-14 | n/a | **done** — the archiving happened; only the doc lags |
| `rupali-husks` | empty directories | dormant | — | `Rupali/Interview` (**0 real files**, one `.DS_Store`), `Rupali/Kaggle` (**0 non-empty files**), `Rupali/docs/unreviewed` (5 files, apparent duplicate pairs `LICENSE`+`LICENSE_1`, `README`+`README_1`, untouched since 2026-02-11) | `find Rupali/Interview Rupali/Kaggle -type f ! -name '.DS_Store' -size +0 \| wc -l` → **0**. Delete-or-keep is Rupali's call; recorded so the decision is visible rather than perpetually deferred | 2026-08-14 | n/a | n/a |

---


### `worktracker-diary` — found 2026-08-15, previously untracked

**An unattended daily agent ran for an unknown period with no row here.** It was found
only by asking why two Obsidian vaults were both written on the same night. This ledger
exists to make that impossible, so the miss is worth stating plainly: `obsidian-collect`
(22:00) was investigated in depth while its **replacement**, running an hour later and
doing the job properly, was invisible.

It is why `obsidian-collect` can be retired: the 22:00 agent writes `Tathya/Obsidian`,
which nothing reads (`sync-manifest.json` targets `Rupali/Obsidian`; the trees are not
hardlinked). Retiring it stops writing dead output.

**It does not close `obsidian-journal`.** For 2026-08-14 the diary reported **35 commits
across 3 repos**; the tree actually saw **95 across 21**. The three it captured are
precisely those with 0–2 unpushed commits — it reports pushed default-branch activity and
so inherits the exact blindness that killed `obsidian-collect`, including the hub repo,
which has no remote at all.


### `doc-structure` — why it exists, and what it deliberately does not measure

Built 2026-08-15 after a concrete failure: `app/(site)/privacy/page.tsx` was rewritten
without reading `docs/content/legal/PRIVACY-CONTENT.md`, the counsel-owned spec that
governs it and that **supersedes** the decision used as authority instead. The edit was
reverted. The propagation edge for that pair was *already declared*, and its `why` already
described the exact failure — propagate is derive-on-demand, and nobody ran it before
editing. The missing thing was a trigger, not a declaration.

**Three numbers, and each was wrong before it was right.** The doc census first reported
2219 docs against a true 1339 (nested workspaces double-counted). The prose-only
supersession baseline was predicted at ~75 and is 107 — the prediction counted *lines*
across 2 roots, the checker counts *files* across all workspaces. Broken-path citations
first reported **603 across 6 projects**; sampling showed `feat/hero-v4-rebuild` (a
branch), `archive/main-2026-05-22` (a tag), `0.0.0.0/0` (a CIDR) and `next/image` (a
module). Narrowed to extension-bearing citations, resolved against project *and* workspace
roots, and split by document kind — a `CHANGELOG` citing a moved file is a record, not a
defect — the honest figure is **50 live-doc broken citations, 308 historical**.

**The table-cell trade, resolved and measured.** Only *backticked* citations are checked
by default. `sanskrit-texts/CLAUDE.md` has **11 dead `Hora/` paths** in a plain markdown
table — every text moved under `Hora/{Jaimini,Nadi,Parashari,Prashna}/` in `1cccca6`
(2026-08-12). `docs --structure --tables` catches all 11, at **+314 tree-wide
(2008 → 2322, ~16%)** — measured at identical scope, after a first pass reported the cost
as "+2275" by comparing a 6-project subset against the whole tree.

It stays opt-in for a reason that outlives that number: **the 2008 baseline is itself
unverified**, dominated by `PanditPawanKaushik` and `WorkTracker`, which are published
content vaults rather than project docs — the same scope boundary `docs --kinds` still
needs. Until that lands neither figure is actionable, and a flag is honest where a
default would not be.

`adoption_date` is blank because it is earned by a human, not asserted by its author.

## Notes on this seed

- Every `active`/`active-unadopted`/`dormant` row above was checked directly
  against the filesystem/launchctl/CLI on 2026-08-10, not copied from the
  brief without verification. The one place the brief's phrasing needed
  correction: **`md-watcher`'s LaunchAgent plist and target script are still
  present on disk and the plist is well-formed** — it is simply not currently
  loaded into launchd, and its logs are an unchanged 22.7MB artifact from
  March. "Dead 10 months" (STATE.md's phrase, referring to the hub
  `.md-watcher/` directory) is correct for that directory; it does not mean
  the LaunchAgent + script pair no longer exist — they do, unloaded but
  intact, which is exactly the "subtler than dead" case the brief warned
  about.
- `propagate-digest`'s `adoption_date` must stay blank until genuine
  engagement is observed for two weeks — filling it in early defeats the
  entire point of this ledger existing.
- 2026-08-14: `propagate`'s row status moved `active` → `retired`, per
  `docs/DECISIONS.md` 2026-08-14. Its `retirement_checklist_done` is
  deliberately left `partial`, not `done` — the code/docs side of the
  retirement is verified here, but the actual `launchctl bootout` was
  explicitly out of scope for this change (done separately, by the parent
  task). Do not mark it `done` on the strength of this entry alone; verify
  no propagate watcher job remains loaded (`launchctl list` shows the digest only) first.
